---
description: Capture an idea in the backlog without breaking flow on current work.
---

Idea: $ARGUMENTS

Append one line to the "Untriaged" section of `.claude/work/backlog.md`: `- $ARGUMENTS (<today's date>)`. Don't start acting on it, don't ask clarifying questions — the point is to capture it in one line and get back to whatever was in progress. `/backlog` is where it gets shaped later.
