---
description: Triage .claude/work/backlog.md — move ideas between Untriaged, Ready, and Someday.
---

Read `.claude/work/backlog.md`. For each item under "Untriaged", ask (briefly, one pass, not one question per item) whether it's:

- **Ready** — clear enough that `/feature` could turn it into a spec today,
- **Someday** — worth keeping but not now,
- or should be dropped entirely.

Move each item into the right section (or delete it) and leave "Untriaged" empty when done. Don't expand items into specs here — that's `/feature`'s job once something is Ready.
