.superpowers/
.next/
node_modules/
.env.local
coverage/
supabase/.branches/
tsconfig.tsbuildinfo
.next-dev-*.log
.next-dev-*.err
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.venv-ml/
ml/artifacts/training_dataset.csv
.scratch/
ml/artifacts/matches*.sqlite
ml/artifacts/matches*.sqlite-journal
ml/artifacts/matches*.sqlite-wal
ml/artifacts/matches*.sqlite-shm
ml/artifacts/win*/
.vercel

# Sibling git worktrees checked out under .claude/ (see CLAUDE.md's Workflow
# note); each is its own checkout with its own node_modules and must never be
# swept in by a broad `.claude` add now that .claude/ has tracked files.
.claude/worktrees/
